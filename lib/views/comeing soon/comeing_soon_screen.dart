import 'package:flutter/material.dart';

class ComeingSoonScreen extends StatelessWidget {
  const ComeingSoonScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("comeing soon screen"),
      ),
    );
  }
}
